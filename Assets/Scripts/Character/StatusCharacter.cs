using UnityEngine;

public class StatusCharacter : MonoBehaviour
{
    public NotificationManager notificationManager;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //notificationManager.ShowNotification("Báo thức", "7:00 AM đi làm, hãy tranh thủ đi sớm hôm nay có việc quan trọng.");
        //notificationManager.ShowNotification("Mới thức dậy", "Cơ thể không được thoải mái cho lắm, có lẽ nên đi vệ sinh cá nhân.");
        //notificationManager.ShowNotification("Đói bụng", "Năng lượng đang dần cạn kiệt, nên bổ sung ngay lập tức.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
